## 4.3 任务与协同模块

> **PRD格式**: Hybrid PRD (4部分结构: Agent能力 + 功能特性 + 人机协作 + 验收标准)
> **优先级**: P0 (核心功能模块)
> **所属版本**: v0.5+ (基础任务管理) → v0.8+ (NLP任务创建)

---

### Part 1: Agent能力 (智能任务管理)

#### 1.1 Agent Profile

**相关Agent**: AssistantAgent, EngineerAgent

**任务管理能力**:
- **任务自动创建**: 识别对话中需要后续跟进的事项，自动创建任务
- **任务NLP解析**: 将自然语言描述解析为结构化任务
- **任务智能提醒**: 基于紧急度和优先级智能安排提醒时间
- **任务关联推荐**: 根据任务内容推荐相关知识和历史案例

**MCP工具清单**:

```typescript
// 1. 任务创建与解析
interface TaskCreationTools {
  // 从对话中自动提取任务
  extractTaskFromConversation(conversationId: string): Task[];

  // 自然语言解析任务
  parseTaskFromNL(naturalLanguage: string): ParsedTask;

  // 创建任务
  createTask(task: TaskInput): Task;

  // 批量创建任务
  batchCreateTasks(tasks: TaskInput[]): Task[];
}

// 2. 任务智能管理
interface TaskManagementTools {
  // 任务优先级推荐
  suggestPriority(task: Task): Priority;

  // 任务分配推荐
  suggestAssignee(task: Task): User;

  // 任务截止时间推荐
  suggestDueDate(task: Task): Date;

  // 任务依赖分析
  analyzeDependencies(taskId: string): TaskDependency[];
}

// 3. 任务协同
interface TaskCollaborationTools {
  // 任务评论
  addTaskComment(taskId: string, comment: string): void;

  // 任务@提醒
  mentionUser(taskId: string, userId: string, message: string): void;

  // 任务状态同步
  syncTaskStatus(taskId: string, status: TaskStatus): void;
}
```

---

#### 1.2 工具详细规格

##### 工具1: extractTaskFromConversation

**功能描述**: 从对话中自动识别需要后续跟进的事项并创建任务

**触发条件**:
- 客服承诺后续跟进 (例如: "我稍后给您回电")
- 问题未完全解决需要升级 (例如: "我需要转给工程师确认")
- 客户提出延期需求 (例如: "明天再联系我")
- 质检发现需要改进的问题

**输入参数**:
| 参数名 | 类型 | 必填 | 说明 | 约束 |
|-------|------|------|------|------|
| conversationId | string | 是 | 对话ID | UUID格式 |
| autoCreate | boolean | 否 | 是否自动创建 | 默认false (仅提取不创建) |

**输出格式**:
```json
{
  "conversationId": "conv-12345",
  "extractedAt": "2025-12-30T10:30:00Z",
  "tasks": [
    {
      "title": "回电客户张三讨论退款事宜",
      "type": "callback",
      "description": "客户对产品不满意，要求退款。需要了解具体原因并提供解决方案",
      "priority": "high",
      "suggestedDueDate": "2025-12-30T18:00:00Z",
      "suggestedAssignee": "csr-001",  // 原客服
      "source": {
        "messageId": "msg-456",
        "content": "好的张先生，我下午给您回电详细沟通退款事宜"
      },
      "confidence": 0.92
    },
    {
      "title": "升级故障问题到工程师",
      "type": "escalation",
      "description": "产品报错E101，初步诊断为驱动问题，但客户反馈已更新驱动仍无效，需工程师介入",
      "priority": "urgent",
      "suggestedDueDate": "2025-12-30T12:00:00Z",
      "suggestedAssignee": "engineer-team",
      "relatedKnowledge": ["kb-e101", "kb-driver-troubleshooting"],
      "confidence": 0.88
    }
  ],
  "totalExtracted": 2
}
```

**提取逻辑**:
```python
def extract_tasks_from_conversation(conversation):
    tasks = []

    # 1. 识别承诺类任务（回电、回复、跟进）
    promises = detect_promises(conversation)
    for promise in promises:
        tasks.append({
            "type": "callback" if "回电" in promise else "followup",
            "title": extract_task_title(promise),
            "dueDate": parse_time_expression(promise),  # "明天"→明天18:00
            "priority": infer_priority(promise)
        })

    # 2. 识别升级类任务
    if needs_escalation(conversation):
        tasks.append({
            "type": "escalation",
            "title": f"升级{conversation.issue}到专家",
            "priority": "urgent",
            "dueDate": datetime.now() + timedelta(hours=2)
        })

    # 3. 识别质检改进任务
    if conversation.inspectionScore < 8.0:
        improvements = conversation.inspection.improvements
        for improvement in improvements:
            tasks.append({
                "type": "improvement",
                "title": f"改进: {improvement}",
                "priority": "normal",
                "assignee": conversation.csrId
            })

    return tasks
```

**性能要求**:
- 响应时间: <2秒
- 提取召回率: >80% (人工标注对比)
- 提取精确率: >75% (避免误报)

---

##### 工具2: parseTaskFromNL

**功能描述**: 将自然语言描述解析为结构化任务

**输入参数**:
| 参数名 | 类型 | 必填 | 说明 | 约束 |
|-------|------|------|------|------|
| naturalLanguage | string | 是 | 自然语言任务描述 | 长度1-500字符 |
| context | object | 否 | 上下文信息 | {conversationId?, customerId?} |

**输出格式**:
```json
{
  "parsed": true,
  "confidence": 0.92,
  "task": {
    "title": "明天下午给张三回电话讨论退款",
    "type": "callback",
    "description": "讨论退款事宜",
    "dueDate": "2025-12-31T15:00:00Z",  // 明天下午
    "priority": "high",
    "relatedCustomer": {
      "name": "张三",
      "id": "cust-789"
    },
    "tags": ["退款", "回电"],
    "estimatedDuration": 30  // 预计30分钟
  },
  "parsedFields": {
    "time": "明天下午",
    "action": "回电话",
    "target": "张三",
    "topic": "退款"
  }
}
```

**解析示例**:

| 输入 | 解析结果 |
|-----|---------|
| "明天提醒我给张三客户回电话讨论退款事宜" | type: callback, dueDate: 明天18:00, customer: 张三, topic: 退款 |
| "下周一交付技术方案给李四" | type: delivery, dueDate: 下周一09:00, customer: 李四, deliverable: 技术方案 |
| "3天后跟进王五的故障是否已解决" | type: followup, dueDate: 3天后18:00, customer: 王五, topic: 故障 |
| "紧急：立即升级这个故障到工程师" | type: escalation, priority: urgent, dueDate: 立即 |

**解析逻辑**:
```python
def parse_task_from_nl(text, context):
    # 1. 命名实体识别 (NER)
    entities = extract_entities(text)
    #   - 时间: "明天", "下周一", "3天后"
    #   - 人名: "张三", "李四"
    #   - 动作: "回电", "交付", "跟进"
    #   - 主题: "退款", "技术方案", "故障"

    # 2. 时间表达式标准化
    due_date = normalize_time_expression(entities.get("time"))
    #   "明天" → datetime.now() + timedelta(days=1)
    #   "下周一" → next_monday()
    #   "3天后" → datetime.now() + timedelta(days=3)

    # 3. 任务类型推断
    task_type = infer_task_type(entities.get("action"))
    #   "回电" → callback
    #   "交付" → delivery
    #   "跟进" → followup
    #   "升级" → escalation

    # 4. 优先级推断
    priority = infer_priority(text)
    #   包含"紧急" → urgent
    #   包含"重要" → high
    #   包含"VIP" → high
    #   默认 → normal

    # 5. 关联客户
    customer = None
    if entities.get("person"):
        customer = search_customer_by_name(entities["person"])

    # 6. 补全缺失字段
    if not due_date:
        due_date = datetime.now() + timedelta(days=1)  # 默认明天

    return {
        "title": text[:50],  # 取前50字符作为标题
        "type": task_type,
        "dueDate": due_date,
        "priority": priority,
        "relatedCustomer": customer,
        "tags": entities.get("topics", [])
    }
```

**性能要求**:
- 响应时间: <1秒
- 解析准确率: >85% (时间、类型、优先级)
- 支持中文时间表达式: "今天/明天/后天/下周X/X天后/X小时后"

---

##### 工具3: suggestPriority

**功能描述**: 基于任务内容和上下文推荐优先级

**输入参数**:
| 参数名 | 类型 | 必填 | 说明 | 约束 |
|-------|------|------|------|------|
| task | Task | 是 | 任务对象 | - |

**输出格式**:
```json
{
  "suggestedPriority": "high",
  "confidence": 0.88,
  "reasons": [
    "任务类型为升级(escalation)，默认高优先级",
    "关联客户为VIP客户",
    "截止时间为今天，时间紧迫"
  ],
  "alternatives": [
    {"priority": "urgent", "probability": 0.12},
    {"priority": "normal", "probability": 0.00}
  ]
}
```

**优先级推断规则**:
```python
def suggest_priority(task):
    priority_score = 0

    # 规则1: 任务类型基础分
    type_scores = {
        "escalation": 8,    # 升级
        "callback": 6,      # 回电
        "followup": 5,      # 跟进
        "improvement": 4,   # 改进
        "delivery": 7       # 交付
    }
    priority_score += type_scores.get(task.type, 5)

    # 规则2: 客户等级加成
    if task.customer and task.customer.level == "VIP":
        priority_score += 2

    # 规则3: 时间紧迫度
    time_left = (task.dueDate - datetime.now()).total_seconds() / 3600
    if time_left < 2:       # <2小时
        priority_score += 3
    elif time_left < 24:    # <1天
        priority_score += 2
    elif time_left < 72:    # <3天
        priority_score += 1

    # 规则4: 关键词加成
    keywords = {
        "紧急": +3,
        "重要": +2,
        "投诉": +3,
        "退款": +2,
        "故障": +1
    }
    for keyword, score in keywords.items():
        if keyword in task.title or keyword in task.description:
            priority_score += score

    # 规则5: 历史逾期记录
    if has_overdue_history(task.assignee):
        priority_score += 1

    # 映射分数到优先级
    if priority_score >= 10:
        return "urgent"
    elif priority_score >= 7:
        return "high"
    elif priority_score >= 4:
        return "normal"
    else:
        return "low"
```

---

##### 工具4: suggestAssignee

**功能描述**: 基于任务内容和团队成员能力推荐负责人

**输入参数**:
| 参数名 | 类型 | 必填 | 说明 | 约束 |
|-------|------|------|------|------|
| task | Task | 是 | 任务对象 | - |

**输出格式**:
```json
{
  "suggestedAssignee": {
    "userId": "csr-001",
    "userName": "张三",
    "confidence": 0.85,
    "reasons": [
      "张三是该客户的主责客服",
      "张三在'退款处理'类任务上平均完成时间最短",
      "张三当前工作负载适中(5/10任务)"
    ]
  },
  "alternatives": [
    {
      "userId": "csr-002",
      "userName": "李四",
      "confidence": 0.72,
      "reasons": ["李四擅长退款处理，成功率95%"]
    }
  ]
}
```

**分配逻辑**:
```python
def suggest_assignee(task):
    candidates = []

    # 策略1: 客户关系优先
    if task.customerId:
        primary_csr = get_customer_primary_csr(task.customerId)
        if primary_csr:
            candidates.append({
                "user": primary_csr,
                "score": 10,
                "reason": "客户主责客服"
            })

    # 策略2: 专长匹配
    task_category = classify_task_category(task)
    specialists = get_specialists(task_category)
    for specialist in specialists:
        candidates.append({
            "user": specialist,
            "score": 8,
            "reason": f"擅长{task_category}类任务"
        })

    # 策略3: 负载均衡
    all_csrs = get_available_csrs()
    for csr in all_csrs:
        workload = get_workload(csr)
        if workload < 10:  # 工作量未饱和
            candidates.append({
                "user": csr,
                "score": 5 + (10 - workload),  # 工作量越少分数越高
                "reason": f"当前工作负载适中({workload}/10)"
            })

    # 策略4: 历史绩效
    for candidate in candidates:
        performance = get_task_performance(candidate["user"], task.type)
        if performance.completion_rate > 0.9:
            candidate["score"] += 3
        if performance.avg_completion_time < task.estimatedDuration:
            candidate["score"] += 2

    # 排序并返回
    candidates.sort(key=lambda x: x["score"], reverse=True)
    return candidates[0] if candidates else None
```

---

### Part 2: 功能特性 (任务管理系统)

#### 2.1 任务列表与看板

**功能描述**: 支持列表视图和看板视图管理任务

**列表视图**:
```
┌──────────────────────────────────────────────────────────────────┐
│ 我的任务 (15)        ▼状态   ▼优先级   ▼类型   🔍搜索             │
├──────────────────────────────────────────────────────────────────┤
│ ☐ [紧急] 回电VIP客户张三讨论退款        到期: 今天18:00  ⚠️逾期  │
│   关联对话: conv-12345 | 创建时间: 2小时前                       │
│                                                                  │
│ ☐ [高优] 升级故障E101到工程师           到期: 明天12:00          │
│   关联对话: conv-67890 | 创建时间: 5小时前                       │
│                                                                  │
│ ☑ [普通] 跟进李四客户安装进度           到期: 昨天18:00  ✅已完成│
│   完成时间: 昨天17:30 | 用时: 2天                                │
│                                                                  │
│ ... (显示12个任务)                                               │
│                                                                  │
│ [加载更多]                                                       │
└──────────────────────────────────────────────────────────────────┘
```

**看板视图** (Kanban Board):
```
┌─────────────┬─────────────┬─────────────┬─────────────┐
│ 待办 (5)    │ 进行中 (3)  │ 待审核 (2)  │ 已完成 (10) │
├─────────────┼─────────────┼─────────────┼─────────────┤
│ 📞回电张三  │ 🔧故障排查  │ 📋方案审核  │ ✅安装完成  │
│ 今天18:00   │ 李四在处理  │ 等待客户确认│ 昨天完成    │
│             │             │             │             │
│ 📧发送方案  │ 📝编写文档  │ 🎯测试验收  │ ✅电话回访  │
│ 明天10:00   │ 王五在处理  │ 等待经理批准│ 前天完成    │
│             │             │             │             │
│ ... (3个)   │ ... (1个)   │             │ ... (8个)   │
│             │             │             │             │
│ [+ 新建任务]│             │             │             │
└─────────────┴─────────────┴─────────────┴─────────────┘
```

**核心功能**:
- [ ] 任务CRUD (创建、查看、编辑、删除)
- [ ] 任务状态流转 (待办→进行中→待审核→已完成)
- [ ] 任务筛选 (按状态、优先级、类型、负责人、截止时间)
- [ ] 任务搜索 (全文搜索)
- [ ] 任务排序 (按优先级、截止时间、创建时间)
- [ ] 批量操作 (批量分配、批量完成、批量删除)
- [ ] 拖拽排序 (看板视图)

---

#### 2.2 自然语言创建任务

**功能描述**: 客服用自然语言快速创建任务，无需填写表单

**UI交互**:
```
┌──────────────────────────────────────────────────────────────┐
│ 快速创建任务                                                  │
├──────────────────────────────────────────────────────────────┤
│ 用自然语言描述任务，AI会自动解析                              │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 明天下午给张三回电话讨论退款事宜                          │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                            [AI解析] [手动填写]│
├──────────────────────────────────────────────────────────────┤
│ AI解析结果 (可编辑):                                         │
│                                                              │
│ 任务标题: [明天下午给张三回电话讨论退款事宜_______________]   │
│ 任务类型: [▼ 回电提醒]                                       │
│ 优先级:   [▼ 高]                                             │
│ 截止时间: [2025-12-31 15:00] 📅                              │
│ 负责人:   [▼ 张三(我)]                                       │
│ 关联客户: [张三 (#cust-789)] 🔗                              │
│ 标签:     [退款] [回电] [+添加]                              │
│ 描述:     [讨论退款事宜_______________________________]       │
│                                                              │
│ [创建任务] [取消]                                            │
└──────────────────────────────────────────────────────────────┘
```

**快捷输入示例**:
- "明天提醒我给张三回电" → 自动解析为回电任务
- "3天后跟进王五的故障" → 自动解析为跟进任务
- "紧急：立即升级这个问题到工程师" → 自动解析为升级任务，优先级urgent

**优势**:
- **速度快**: 一句话创建任务，比表单快5倍
- **准确性高**: AI解析准确率>85%
- **可编辑**: 解析结果可手动调整
- **学习能力**: 根据用户修改反馈，持续优化解析准确度

---

#### 2.3 任务提醒与通知

**功能描述**: 多渠道任务提醒，避免遗漏

**提醒渠道**:
1. **站内通知** (系统消息)
2. **桌面弹窗** (浏览器通知)
3. **邮件提醒** (可配置)
4. **飞书/企微机器人** (v1.0)

**提醒策略**:

| 优先级 | 提醒时间 | 提醒频率 |
|-------|---------|---------|
| urgent (紧急) | 创建时立即, 截止前30分钟, 截止时 | 每10分钟提醒 |
| high (高) | 截止前2小时, 截止前30分钟, 截止时 | 每30分钟提醒 |
| normal (普通) | 截止前1天, 截止前2小时, 截止时 | 每2小时提醒 |
| low (低) | 截止前1天, 截止时 | 仅提醒2次 |

**提醒内容示例**:
```
┌─────────────────────────────────────────┐
│ 🔔 任务提醒                              │
├─────────────────────────────────────────┤
│ [紧急] 回电VIP客户张三讨论退款          │
│                                         │
│ 截止时间: 今天18:00 (还剩30分钟)        │
│ 优先级: 紧急                            │
│ 关联对话: conv-12345                    │
│                                         │
│ [立即处理] [推迟1小时] [标记完成]       │
└─────────────────────────────────────────┘
```

**逾期处理**:
- 任务逾期后，优先级自动提升1级 (normal → high → urgent)
- 逾期任务在任务列表顶部显示，标记⚠️
- 逾期任务每小时提醒1次，直到完成

---

#### 2.4 任务关联与上下文

**功能描述**: 任务与对话、客户、知识关联，提供完整上下文

**关联信息**:

1. **关联对话**:
```
┌─────────────────────────────────────────┐
│ 任务: 回电张三讨论退款                   │
├─────────────────────────────────────────┤
│ 关联对话: conv-12345                     │
│ • 客户: 张三 (VIP)                       │
│ • 渠道: 飞书                             │
│ • 对话时间: 2025-12-30 10:00             │
│ • 问题类型: 退款咨询                     │
│                                         │
│ [查看完整对话]                           │
└─────────────────────────────────────────┘
```

2. **关联知识**:
```
Agent自动推荐相关知识:
- 📘 退款流程指南 (引用次数: 120)
- 📘 退款常见问题FAQ (引用次数: 85)
- 📘 VIP客户退款特殊政策 (引用次数: 45)
```

3. **历史任务**:
```
该客户的历史任务:
- ✅ 2025-12-20: 安装指导 (已完成)
- ✅ 2025-12-15: 故障排查 (已完成)
- ⚠️ 发现: 该客户近期咨询频繁，建议主动关怀
```

---

#### 2.5 任务协作与评论

**功能描述**: 团队成员可在任务下评论、@提醒、附件上传

**评论功能**:
```
┌─────────────────────────────────────────────────────┐
│ 任务: 升级故障E101到工程师                          │
├─────────────────────────────────────────────────────┤
│ 评论 (5):                                           │
│                                                     │
│ 张三 (客服)  2小时前                                │
│ 客户反馈已更新驱动仍无效，疑似硬件问题              │
│                                                     │
│ 李四 (工程师) 1小时前                               │
│ @张三 请让客户拍摄设备指示灯照片发给我              │
│                                                     │
│ 张三 (客服)  30分钟前                               │
│ [📎 照片.jpg] 已上传                                 │
│                                                     │
│ 李四 (工程师) 10分钟前                              │
│ 确认是硬件故障，建议换货。我已创建换货工单。        │
│                                                     │
│ ┌───────────────────────────────────────────────┐   │
│ │ 添加评论...                                   │   │
│ │ 支持@提醒、上传附件                           │   │
│ └───────────────────────────────────────────────┘   │
│ [发送] [@提醒] [📎上传附件]                         │
└─────────────────────────────────────────────────────┘
```

**@提醒功能**:
- 输入`@`自动弹出团队成员列表
- 被@的用户收到站内通知和邮件提醒
- 支持@多人

**附件支持**:
- 图片: jpg, png, gif (最大10MB)
- 文档: pdf, doc, docx, xls, xlsx (最大20MB)
- 压缩包: zip, rar (最大50MB)

---

#### 2.6 任务模板

**功能描述**: 预设常用任务模板，快速创建标准化任务

**预设模板**:

1. **客户回电模板**:
```yaml
模板名称: 客户回电
默认字段:
  type: callback
  priority: normal
  dueDate: +1天
  estimatedDuration: 15分钟
  tags: [回电]
```

2. **故障升级模板**:
```yaml
模板名称: 故障升级到工程师
默认字段:
  type: escalation
  priority: high
  assignee: engineer-team
  dueDate: +2小时
  tags: [故障, 升级]
  checklist:
    - [ ] 收集故障现象描述
    - [ ] 记录错误代码
    - [ ] 确认客户系统环境
    - [ ] 提供初步诊断结果
```

3. **质检改进模板**:
```yaml
模板名称: 质检改进任务
默认字段:
  type: improvement
  priority: normal
  dueDate: +3天
  tags: [质检, 改进]
  description: 根据质检结果改进服务质量
```

**自定义模板**:
- 管理员可创建团队专属模板
- 个人可保存常用任务为个人模板

---

### Part 3: 人机协作边界

#### 3.1 AI主导场景

**场景1: 自动创建任务**
- **AI职责**: 从对话中自动识别需要跟进的事项，创建任务草稿
- **人工职责**: 审核任务草稿，确认或修改后正式创建
- **协作方式**: AI提取 → 人工审核 → 确认创建

**示例**:
```
对话结束后，AssistantAgent分析:
  ↓
发现客服承诺: "我稍后给您回电"
  ↓
自动创建任务草稿:
  - 标题: 回电客户张三
  - 类型: callback
  - 截止时间: 今天18:00
  ↓
弹窗提示客服: "检测到待跟进事项，是否创建任务?"
  ↓
客服确认 → 正式创建任务
客服修改 → 调整后创建
客服拒绝 → 丢弃草稿
```

---

**场景2: 智能任务分配**
- **AI职责**: 基于任务内容、团队成员能力、工作负载推荐负责人
- **人工职责**: 确认或手动调整负责人
- **协作方式**: AI推荐 → 人工确认 → 自动分配

**示例**:
```
新任务: "升级故障E101到工程师"
  ↓
AI分析:
  - 任务类型: 技术故障
  - 专长匹配: 工程师李四擅长驱动问题(90%解决率)
  - 工作负载: 李四当前3个任务，负载适中
  ↓
推荐负责人: 李四 (置信度88%)
  ↓
创建任务时自动填充:
  负责人: [李四 (推荐)] ▼
  ↓
用户可保持或手动修改
```

---

**场景3: 任务优先级调整**
- **AI职责**: 实时监控任务状态，动态调整优先级
- **人工职责**: 确认优先级调整，或手动覆盖
- **协作方式**: AI监控 → 发现异常 → 推荐调整 → 人工确认

**示例**:
```
任务: "跟进王五的故障"
原优先级: normal
截止时间: 明天18:00
  ↓
AI监控发现:
  - 距离截止时间<2小时
  - 任务仍未开始处理
  - 关联客户为VIP
  ↓
推送通知: "建议将任务优先级提升至'高'"
  ↓
用户确认 → 优先级提升
用户拒绝 → 保持原优先级
```

---

#### 3.2 人工主导场景

**场景1: 复杂任务规划**
- **人工职责**: 将复杂任务拆分为多个子任务
- **AI职责**: 提供拆分建议和模板
- **协作方式**: 人工主导拆分 → AI辅助规划

**示例**:
```
创建任务: "产品V2.0上线准备"
  ↓
人工判断: 这是复杂任务，需要拆分
  ↓
AI提供建议:
  - 子任务1: 编写上线文档 (技术)
  - 子任务2: 更新知识库 (客服)
  - 子任务3: 客服培训 (培训)
  - 子任务4: 公告发布 (运营)
  ↓
人工调整:
  - 补充: 测试验收
  - 修改: 客服培训改为线上视频
  ↓
确认创建4个子任务，设置依赖关系
```

---

**场景2: 紧急任务插队**
- **人工职责**: 识别紧急突发任务，手动插队
- **AI职责**: 提示被影响的任务，建议调整计划
- **协作方式**: 人工决策 → AI重新规划

**示例**:
```
突发: VIP客户投诉，需立即处理
  ↓
客服手动创建紧急任务:
  - 优先级: urgent
  - 截止时间: 立即
  ↓
AI检测到:
  - 当前正在处理的任务: "编写周报" (normal)
  - 建议: "暂停当前任务，优先处理投诉"
  ↓
客服确认 → 暂停周报，处理投诉
  ↓
AI自动调整:
  - "编写周报"截止时间延后2小时
  - 向相关人员发送通知
```

---

#### 3.3 协作模式场景

**场景1: 任务进度跟踪**
- **AI职责**: 自动检测任务进度，发现延误风险
- **人工职责**: 解释延误原因，决定是否调整
- **协作方式**: AI监控 → 人工解释 → 共同调整

**示例**:
```
任务: "3天后跟进客户安装进度"
创建时间: 2025-12-27
截止时间: 2025-12-30 18:00
  ↓
Day 1: AI检测 - 任务未开始 (正常)
Day 2: AI检测 - 任务未开始 (轻度提醒)
Day 3 10:00: AI检测 - 任务仍未开始 (告警)
  ↓
推送通知: "任务即将逾期，还剩8小时"
  ↓
客服回复: "客户昨天取消了安装，暂不需要跟进"
  ↓
客服标记任务为"已取消"
  ↓
AI记录: 客户取消安装场景，无需持续提醒
```

---

**场景2: 知识关联推荐**
- **AI职责**: 根据任务内容推荐相关知识
- **人工职责**: 选择有用的知识，补充到任务描述
- **协作方式**: AI推荐 → 人工筛选 → 引用到任务

**示例**:
```
任务: "回电客户讨论退款"
  ↓
AI自动推荐相关知识:
  1. 📘 退款流程指南 (相关度95%)
  2. 📘 VIP客户退款政策 (相关度88%)
  3. 📘 退款常见问题FAQ (相关度82%)
  ↓
客服查看推荐:
  - 选择知识1、2 → 添加到任务备注
  - 忽略知识3 (不相关)
  ↓
任务备注自动填充:
  "参考资料: 退款流程指南、VIP客户退款政策"
```

---

### Part 4: 验收标准

#### 4.1 功能验收

**任务提取**:
- [ ] extractTaskFromConversation提取召回率>80%
- [ ] extractTaskFromConversation提取精确率>75%
- [ ] 提取响应时间<2秒

**NLP解析**:
- [ ] parseTaskFromNL解析准确率>85%
- [ ] 支持至少10种中文时间表达式
- [ ] 解析响应时间<1秒

**任务管理**:
- [ ] 任务CRUD 100%可用
- [ ] 支持列表视图和看板视图切换
- [ ] 任务筛选、搜索、排序功能正常

**任务提醒**:
- [ ] 站内通知、桌面弹窗100%可用
- [ ] 提醒时间误差<1分钟
- [ ] 逾期任务自动标记

**任务协作**:
- [ ] 评论、@提醒、附件上传功能正常
- [ ] @提醒即时推送
- [ ] 附件上传支持图片/文档/压缩包

---

#### 4.2 性能验收

| 指标 | 目标值 | 测量方法 | 优先级 |
|-----|--------|---------|--------|
| 任务列表加载 | <1秒 (100条) | 前端埋点 | P0 |
| 任务创建响应 | <500ms | API监控 | P0 |
| NLP解析响应 | <1秒 | Prometheus | P0 |
| 任务提醒延迟 | <10秒 | 系统日志 | P0 |
| 看板拖拽响应 | <200ms | 前端性能 | P1 |
| 并发任务创建 | 100 QPS | 压测 | P1 |

---

#### 4.3 准确性验收

**NLP解析准确性**:
- [ ] 时间解析准确率>90% (人工标注100条)
- [ ] 任务类型推断准确率>85%
- [ ] 优先级推断准确率>80%

**任务分配准确性**:
- [ ] 自动分配与人工分配一致性>75%
- [ ] 工作负载计算准确率100%

**提醒准确性**:
- [ ] 提醒时间误差<1分钟
- [ ] 提醒遗漏率<0.1% (1000次提醒<1次遗漏)

---

#### 4.4 用户体验验收

**客服端**:
- [ ] 自然语言创建任务比表单创建快5倍
- [ ] 任务提醒及时且不打扰
- [ ] 任务列表一目了然

**管理端**:
- [ ] 团队任务看板清晰展示工作状态
- [ ] 任务统计数据准确

**用户满意度**:
- [ ] 客服对任务管理功能满意度≥4.2/5.0
- [ ] NLP创建任务采用率≥60%

---

#### 4.5 业务验收

**效率提升**:
- [ ] 任务创建时间: 2分钟(表单) → 20秒(NLP) (提升6倍)
- [ ] 任务遗漏率: 15% → 5% (下降67%)
- [ ] 任务逾期率: 20% → 10% (下降50%)

**协作提升**:
- [ ] 团队任务可见性: 30% → 100% (提升233%)
- [ ] 跨部门协作效率: 提升40% (通过@提醒和评论)

---

## 附录

### A. 任务类型定义

```typescript
enum TaskType {
  callback = "callback",           // 回电提醒
  followup = "followup",           // 跟进任务
  escalation = "escalation",       // 升级任务
  delivery = "delivery",           // 交付任务
  improvement = "improvement",     // 改进任务
  training = "training",           // 培训任务
  documentation = "documentation", // 文档任务
  other = "other"                  // 其他
}
```

---

### B. 任务优先级定义

```typescript
enum TaskPriority {
  urgent = "urgent",   // 紧急 (立即处理)
  high = "high",       // 高 (今天必须处理)
  normal = "normal",   // 普通 (本周处理)
  low = "low"          // 低 (有空处理)
}
```

---

### C. 任务状态流转

```
待办(pending) → 进行中(in_progress) → 待审核(review) → 已完成(done)
                                    ↓
                                  已取消(cancelled)
```

---

### D. 数据库索引优化

```sql
-- Task表索引
CREATE INDEX idx_task_assignee ON tasks(assignee_id);
CREATE INDEX idx_task_due_date ON tasks(due_date);
CREATE INDEX idx_task_status ON tasks(status);
CREATE INDEX idx_task_priority ON tasks(priority);

-- 复合索引（用于任务列表查询）
CREATE INDEX idx_task_assignee_status_due ON tasks(assignee_id, status, due_date);

-- 全文搜索索引
CREATE INDEX idx_task_fulltext ON tasks USING GIN(to_tsvector('chinese', title || ' ' || description));
```

---

**文档结束**

**相关文档**:
- AssistantAgent PRD: `3-agents/AssistantAgent-PRD.md`
- EngineerAgent PRD: `3-agents/EngineerAgent-PRD.md`
- v0.5增量PRD: `../../3-incremental/PRD-v0.5-Incremental.md`
- v0.8增量PRD: `../../3-incremental/PRD-v0.8-Incremental.md`
