## 4.2 分析与质检模块

> **PRD格式**: Hybrid PRD (4部分结构: Agent能力 + 功能特性 + 人机协作 + 验收标准)
> **优先级**: P0 (核心功能模块)
> **所属版本**: v0.5+ (质检基础) → v0.8+ (智能分析增强)

---

### Part 1: Agent能力 (InspectorAgent数据分析)

#### 1.1 Agent Profile

**Agent Name**: InspectorAgent (质检与分析专员)

**角色定位**: 智能质检员 + 数据分析师

**核心能力**:
- **五维度质检**: 响应速度、专业性、友好度、合规性、解决率
- **违规检测**: 敏感词、禁用话术、承诺超限、服务态度
- **数据分析**: 趋势分析、绩效对比、问题归因
- **报告生成**: 质检报告、周报/月报、改进建议

**MCP工具清单**:

```typescript
// 1. 质检核心工具
interface InspectorTools {
  // 五维度质检
  inspectConversation(conversationId: string): InspectionResult;

  // 批量质检
  batchInspect(conversationIds: string[], samplingRate?: number): BatchInspectionResult;

  // 违规检测
  detectViolation(conversationId: string): ViolationReport;

  // 质检申诉处理
  reviewAppeal(appealId: string, reviewerComment: string): AppealResult;
}

// 2. 数据分析工具
interface AnalyticsTools {
  // 生成质检报告
  generateQualityReport(timeRange: TimeRange, filters: ReportFilters): QualityReport;

  // 团队绩效对比
  compareTeamPerformance(teamId: string, timeRange: TimeRange): PerformanceComparison;

  // 趋势分析
  analyzeTrend(metric: string, timeRange: TimeRange): TrendAnalysis;

  // 根因分析
  analyzeRootCause(issue: string, timeRange: TimeRange): RootCauseReport;

  // 改进建议生成
  recommendImprovement(inspectionResult: InspectionResult): ImprovementPlan;
}

// 3. 实时监控工具
interface MonitoringTools {
  // 实时质检统计
  getRealTimeStats(): RealTimeQAStats;

  // 异常检测
  detectAnomaly(metric: string, threshold: number): AnomalyAlert[];

  // 告警推送
  sendAlert(alertType: string, target: string, content: string): void;
}
```

---

#### 1.2 工具详细规格

##### 工具1: inspectConversation

**功能描述**: 对单个对话进行五维度质检评分

**输入参数**:
| 参数名 | 类型 | 必填 | 说明 | 约束 |
|-------|------|------|------|------|
| conversationId | string | 是 | 对话ID | UUID格式 |
| mode | string | 否 | 质检模式 | auto(自动)/manual(人工触发)，默认auto |

**输出格式**:
```json
{
  "conversationId": "conv-12345",
  "inspectedAt": "2025-12-30T10:30:00Z",
  "inspector": "InspectorAgent",
  "scores": {
    "responseSpeed": 8.5,      // 响应速度 (0-10)
    "professionalism": 9.0,    // 专业性 (0-10)
    "friendliness": 8.0,       // 友好度 (0-10)
    "compliance": 10.0,        // 合规性 (0-10)
    "resolutionRate": 9.5      // 解决率 (0-10)
  },
  "overallScore": 9.0,         // 综合得分
  "violations": [
    {
      "type": "sensitive_word",
      "severity": "low",
      "content": "客服使用了'绝对'一词，存在承诺超限风险",
      "suggestion": "避免使用绝对化表述，改为'通常情况下'"
    }
  ],
  "highlights": [
    "客服在第3轮主动提供了多种解决方案，体现专业性",
    "响应时间平均30秒，表现优秀"
  ],
  "improvements": [
    "建议在问候语中增加客户姓名，提升个性化服务",
    "第5轮回复略显生硬，可适当使用表情符号增加友好度"
  ],
  "category": "excellent",     // excellent|good|average|poor
  "needHumanReview": false
}
```

**性能要求**:
- 响应时间: <3秒 (P95)
- 准确率: >85% (与人工评分对比)
- 并发支持: 10 QPS

---

##### 工具2: batchInspect

**功能描述**: 批量质检，支持采样策略

**输入参数**:
| 参数名 | 类型 | 必填 | 说明 | 约束 |
|-------|------|------|------|------|
| conversationIds | string[] | 是 | 对话ID列表 | 最多100个 |
| samplingRate | number | 否 | 采样率 | 0-1，默认1.0 (100%) |
| priority | string | 否 | 优先级 | low/normal/high，默认normal |

**输出格式**:
```json
{
  "batchId": "batch-67890",
  "totalCount": 50,
  "inspectedCount": 50,
  "sampledCount": 25,        // 如果samplingRate=0.5
  "averageScore": 8.5,
  "distribution": {
    "excellent": 15,
    "good": 25,
    "average": 8,
    "poor": 2
  },
  "topIssues": [
    {"issue": "响应速度慢", "count": 5},
    {"issue": "专业性不足", "count": 3}
  ],
  "results": [/* 各对话质检结果 */]
}
```

**采样策略**:
```python
def sampling_strategy(conversation):
    # 优先级1: 新员工对话 - 100%采样
    if conversation.csrTenure < 90:  # 入职<3个月
        return True

    # 优先级2: VIP客户对话 - 100%采样
    if conversation.customerLevel == "VIP":
        return True

    # 优先级3: 投诉对话 - 100%采样
    if conversation.hasComplaint:
        return True

    # 优先级4: 资深员工 - 20%采样
    if conversation.csrTenure > 365:  # 入职>1年
        return random.random() < 0.2

    # 优先级5: 普通员工 - 50%采样
    return random.random() < 0.5
```

---

##### 工具3: generateQualityReport

**功能描述**: 自动生成质检报告（周报/月报）

**输入参数**:
| 参数名 | 类型 | 必填 | 说明 | 约束 |
|-------|------|------|------|------|
| timeRange | object | 是 | 时间范围 | {start: Date, end: Date} |
| filters | object | 否 | 过滤条件 | {teamId?, csrId?, channel?} |
| reportType | string | 是 | 报告类型 | daily/weekly/monthly |

**输出格式**:
```json
{
  "reportId": "report-2025-12-week52",
  "reportType": "weekly",
  "timeRange": {
    "start": "2025-12-23T00:00:00Z",
    "end": "2025-12-29T23:59:59Z"
  },
  "summary": {
    "totalConversations": 500,
    "inspectedConversations": 350,
    "averageScore": 8.3,
    "scoreChange": "+0.5",     // 相比上周
    "excellentRate": 0.40,     // 40%优秀
    "violationRate": 0.05      // 5%违规
  },
  "dimensionScores": {
    "responseSpeed": 8.5,
    "professionalism": 8.2,
    "friendliness": 8.0,
    "compliance": 9.5,
    "resolutionRate": 8.1
  },
  "topPerformers": [
    {"csrName": "张三", "score": 9.5, "rank": 1},
    {"csrName": "李四", "score": 9.2, "rank": 2}
  ],
  "bottomPerformers": [
    {"csrName": "王五", "score": 6.8, "rank": 48},
    {"csrName": "赵六", "score": 7.0, "rank": 47}
  ],
  "commonIssues": [
    {
      "issue": "响应速度慢(>2分钟)",
      "count": 25,
      "affectedCSRs": ["王五", "赵六"],
      "recommendation": "建议增加快捷回复模板，减少输入时间"
    },
    {
      "issue": "专业术语解释不清",
      "count": 18,
      "affectedCSRs": ["新员工A", "新员工B"],
      "recommendation": "建议参加产品知识培训"
    }
  ],
  "trendAnalysis": {
    "trend": "improving",      // improving/stable/declining
    "keyFactors": [
      "知识库完善度提升，知识引用次数增加30%",
      "AssistantAgent推荐准确率提升至88%"
    ]
  },
  "recommendations": [
    "继续优化知识库，重点补充故障排查类知识",
    "对响应速度慢的客服进行专项辅导",
    "建议引入快捷回复功能减少打字时间"
  ]
}
```

**生成逻辑**:
```python
def generate_quality_report(timeRange, filters):
    # 1. 聚合数据
    conversations = query_conversations(timeRange, filters)
    inspections = query_inspections(conversations)

    # 2. 计算统计指标
    summary = calculate_summary(inspections)
    dimensionScores = calculate_dimension_scores(inspections)

    # 3. 排名分析
    topPerformers = rank_csrs(inspections, order="desc", limit=10)
    bottomPerformers = rank_csrs(inspections, order="asc", limit=10)

    # 4. 问题归因
    commonIssues = extract_common_issues(inspections)

    # 5. 趋势分析（对比上一周期）
    previousReport = query_previous_report(timeRange, filters)
    trendAnalysis = compare_with_previous(summary, previousReport)

    # 6. AI生成改进建议
    recommendations = generate_recommendations_by_llm({
        "summary": summary,
        "commonIssues": commonIssues,
        "trend": trendAnalysis
    })

    return {
        "summary": summary,
        "dimensionScores": dimensionScores,
        "topPerformers": topPerformers,
        "bottomPerformers": bottomPerformers,
        "commonIssues": commonIssues,
        "trendAnalysis": trendAnalysis,
        "recommendations": recommendations
    }
```

---

##### 工具4: compareTeamPerformance

**功能描述**: 横向对比团队成员绩效

**输入参数**:
| 参数名 | 类型 | 必填 | 说明 | 约束 |
|-------|------|------|------|------|
| teamId | string | 是 | 团队ID | - |
| timeRange | object | 是 | 时间范围 | - |
| metrics | string[] | 否 | 对比维度 | 默认全部维度 |

**输出格式**:
```json
{
  "teamId": "team-sales-01",
  "teamName": "销售支持组",
  "memberCount": 10,
  "timeRange": {"start": "2025-12-01", "end": "2025-12-31"},
  "teamAverage": 8.3,
  "comparison": [
    {
      "csrId": "csr-001",
      "csrName": "张三",
      "score": 9.5,
      "rank": 1,
      "deviation": "+1.2",      // 相比团队平均
      "strengths": ["响应速度", "专业性"],
      "weaknesses": []
    },
    {
      "csrId": "csr-002",
      "csrName": "李四",
      "score": 8.8,
      "rank": 2,
      "deviation": "+0.5",
      "strengths": ["友好度", "解决率"],
      "weaknesses": ["响应速度略慢"]
    }
  ],
  "insights": [
    "张三在响应速度维度领先团队，建议分享最佳实践",
    "王五在专业性维度落后，建议参加产品培训"
  ]
}
```

---

##### 工具5: analyzeTrend

**功能描述**: 分析指标趋势（日/周/月）

**输入参数**:
| 参数名 | 类型 | 必填 | 说明 | 约束 |
|-------|------|------|------|------|
| metric | string | 是 | 指标名称 | averageScore/responseSpeed/satisfactionRate等 |
| timeRange | object | 是 | 时间范围 | - |
| granularity | string | 否 | 粒度 | day/week/month，默认day |

**输出格式**:
```json
{
  "metric": "averageScore",
  "granularity": "day",
  "dataPoints": [
    {"date": "2025-12-23", "value": 8.2},
    {"date": "2025-12-24", "value": 8.3},
    {"date": "2025-12-25", "value": 8.5},
    {"date": "2025-12-26", "value": 8.4},
    {"date": "2025-12-27", "value": 8.6},
    {"date": "2025-12-28", "value": 8.7},
    {"date": "2025-12-29", "value": 8.8}
  ],
  "trend": "improving",         // improving/stable/declining
  "changeRate": "+7.3%",        // 相比首日
  "forecast": {
    "next7Days": 8.9,
    "confidence": 0.85
  },
  "correlations": [
    {
      "factor": "知识库引用次数",
      "correlation": 0.82,
      "insight": "知识库引用增加与质检得分提升呈强正相关"
    }
  ]
}
```

---

### Part 2: 功能特性 (BI看板与报表)

#### 2.1 实时BI看板

**功能描述**: 高管/经理视角的实时业务监控看板

**技术方案**: Grafana + Prometheus + PostgreSQL

**核心指标面板**:

```yaml
Dashboard: 智能售后实时看板
Panels:
  # 1. 核心业务指标
  - 今日对话量:
      type: SingleStat
      query: sum(conversations_count{status="all"})
      compare: 昨日同期 (+15%)

  - 平均响应时间:
      type: Gauge
      query: avg(response_time_seconds)
      threshold:
        green: <60s
        yellow: 60-120s
        red: >120s

  - 客户满意度:
      type: Gauge
      query: avg(satisfaction_score)
      current: 4.5/5.0
      target: 4.6/5.0

  - 质检平均分:
      type: Gauge
      query: avg(inspection_overall_score)
      current: 8.3/10
      trend: +0.5 (本周)

  # 2. 趋势图表
  - 对话量趋势(7天):
      type: TimeSeries
      metrics:
        - 总对话量
        - AI辅助对话量
        - 纯人工对话量

  - 质检得分趋势(30天):
      type: TimeSeries
      dimensions:
        - 响应速度
        - 专业性
        - 友好度
        - 合规性
        - 解决率

  # 3. 分布统计
  - 质检评级分布:
      type: PieChart
      data:
        excellent: 40%
        good: 45%
        average: 12%
        poor: 3%

  - 违规类型分布:
      type: BarChart
      data:
        敏感词: 5
        承诺超限: 3
        服务态度: 2
        信息泄露: 1

  # 4. 排名榜单
  - 客服绩效排名TOP10:
      type: Table
      columns: [姓名, 对话量, 满意度, 质检得分, 知识贡献]

  - 知识引用排行TOP10:
      type: Table
      columns: [知识标题, 引用次数, 解决率, 满意度]

  # 5. 告警通知
  - 实时告警:
      type: AlertList
      alerts:
        - [严重] 质检发现违规行为 (客服:王五, 对话ID:conv-123)
        - [警告] 平均响应时间超过2分钟 (当前:2.5分钟)
        - [提示] VIP客户等待超过1分钟 (客户ID:cust-456)
```

**访问权限**:
- **超级管理员**: 全部数据
- **团队经理**: 本团队数据
- **普通客服**: 个人数据
- **质检专员**: 全部对话质检数据

**刷新频率**:
- 实时指标: 5秒刷新
- 趋势图表: 30秒刷新
- 排名榜单: 5分钟刷新

---

#### 2.2 自定义报表生成

**功能描述**: 管理员自定义报表维度和指标，导出Excel/PDF

**UI界面**:
```
┌─────────────────────────────────────────────────────┐
│ 自定义报表生成                                        │
├─────────────────────────────────────────────────────┤
│ 报表名称: [质检月报_2025年12月____________________]   │
│                                                       │
│ 时间范围: [2025-12-01] 至 [2025-12-31]                │
│                                                       │
│ 筛选条件:                                             │
│   团队: [▼ 全部团队]  客服: [▼ 全部客服]              │
│   渠道: [▼ 全部渠道]  客户等级: [▼ 全部等级]          │
│                                                       │
│ 报表维度: (多选)                                      │
│   ☑ 按客服统计    ☑ 按团队统计                       │
│   ☑ 按渠道统计    ☐ 按客户等级统计                   │
│                                                       │
│ 统计指标: (多选)                                      │
│   ☑ 对话量        ☑ 平均响应时间                     │
│   ☑ 客户满意度    ☑ 质检得分                         │
│   ☑ 违规次数      ☑ 知识引用次数                     │
│                                                       │
│ 导出格式: ◉ Excel   ○ PDF   ○ CSV                    │
│                                                       │
│ [生成报表] [预览] [取消]                              │
└─────────────────────────────────────────────────────┘
```

**报表模板**:

1. **质检周报模板**:
```markdown
# 质检周报 (2025年第52周)

## 一、整体概况
- 总对话量: 500条
- 质检覆盖: 350条 (70%)
- 平均得分: 8.3/10 (↑0.5)
- 优秀率: 40% (↑5%)

## 二、五维度得分
| 维度 | 本周得分 | 上周得分 | 变化 |
|-----|---------|---------|------|
| 响应速度 | 8.5 | 8.2 | +0.3 |
| 专业性 | 8.2 | 8.0 | +0.2 |
| 友好度 | 8.0 | 7.8 | +0.2 |
| 合规性 | 9.5 | 9.3 | +0.2 |
| 解决率 | 8.1 | 7.9 | +0.2 |

## 三、客服排名
[TOP10表格]

## 四、常见问题
[问题列表]

## 五、改进建议
[AI生成的建议]
```

2. **客服绩效报表模板**:
```markdown
# 客服绩效月报 - 张三 (2025年12月)

## 个人表现
- 对话量: 120条 (团队排名: 3/10)
- 满意度: 4.6/5.0 (团队排名: 2/10)
- 质检得分: 9.2/10 (团队排名: 1/10)
- 知识贡献: 8条 (团队排名: 2/10)

## 强项分析
- ✅ 响应速度快 (平均25秒，优于团队平均)
- ✅ 专业性强 (故障诊断准确率92%)

## 改进方向
- ⚠️ 友好度略显不足，建议增加表情符号使用
- ⚠️ 知识沉淀意识待提升

## 本月亮点
- 成功解决复杂故障案例3起
- 获得客户感谢2次
```

---

#### 2.3 绩效排名与激励

**功能描述**: 实时排名榜单，激励客服竞争

**排名维度**:

1. **综合绩效排名**:
```
积分 = 对话量×0.2 + 满意度×0.3 + 质检得分×0.3 + 知识贡献×0.2
```

2. **单项排名**:
- 对话量排名
- 满意度排名
- 质检得分排名
- 响应速度排名
- 知识贡献排名 (沉淀知识数)

**可视化展示**:
```
┌─────────────────────────────────────────┐
│ 本周绩效排行榜 (2025-W52)               │
├─────────────────────────────────────────┤
│ 🥇 1. 张三    综合得分: 95.2           │
│    对话120条 | 满意度4.8 | 质检9.5     │
│                                         │
│ 🥈 2. 李四    综合得分: 92.8           │
│    对话110条 | 满意度4.7 | 质检9.2     │
│                                         │
│ 🥉 3. 王五    综合得分: 88.5           │
│    对话105条 | 满意度4.5 | 质检8.8     │
│                                         │
│ ... (展示TOP10)                         │
│                                         │
│ 你的排名: 第5名 (↑2名)                 │
│ 继续加油，距离前3名还差5.2分!           │
└─────────────────────────────────────────┘
```

**激励机制**:
- **月度MVP**: 综合排名第1，奖金¥1000
- **进步之星**: 排名提升最多，奖金¥500
- **知识达人**: 知识贡献最多，奖金¥500
- **满意度之星**: 客户满意度最高，奖金¥500

---

#### 2.4 数据导出与分享

**功能描述**: 支持多种格式导出，便于汇报和存档

**导出格式**:

1. **Excel导出**:
```
文件名: 质检报告_2025年12月_销售组.xlsx
包含Sheet:
  - 概览 (Summary)
  - 客服排名 (Ranking)
  - 五维度得分 (Dimension Scores)
  - 违规记录 (Violations)
  - 原始数据 (Raw Data)
```

2. **PDF导出**:
```
文件名: 质检报告_2025年12月_销售组.pdf
包含章节:
  - 封面 (带公司Logo)
  - 目录
  - 执行摘要
  - 详细分析
  - 数据图表
  - 附录
```

3. **CSV导出**:
```
文件名: inspection_data_202512.csv
包含字段:
conversationId,csrName,inspectionDate,overallScore,responseSpeed,professionalism,...
```

**分享功能**:
- **邮件发送**: 定时发送周报/月报到指定邮箱
- **链接分享**: 生成加密链接，7天有效
- **权限控制**: 仅团队经理及以上可导出全员数据

---

### Part 3: 人机协作边界

#### 3.1 AI主导场景

**场景1: 日常质检**
- **AI职责**: 自动质检所有对话，生成五维度评分和改进建议
- **人工职责**: 抽查复核，处理申诉
- **协作方式**: AI质检 → 低分对话人工复核 → AI学习人工判断

**示例**:
```
对话conv-12345 (客服:张三)
  ↓
InspectorAgent自动质检
  ↓
得分: 7.5/10 (低于8分阈值)
  ↓
自动触发人工复核任务
  ↓
质检专员王经理复核
  ↓
确认得分准确，批准
  ↓
InspectorAgent记录人工反馈，优化模型
```

---

**场景2: 趋势分析**
- **AI职责**: 自动分析数据趋势，生成图表和洞察
- **人工职责**: 解读趋势原因，制定改进措施
- **协作方式**: AI分析 → 人工决策 → AI执行

**示例**:
```
AI检测到: 本周平均响应时间增加15%
  ↓
AI分析根因:
  - 新员工占比增加20%
  - 复杂故障咨询增加30%
  ↓
推送告警给团队经理
  ↓
经理决策:
  - 安排老员工带教新员工
  - 补充故障排查知识到知识库
  ↓
AI执行: 自动分配带教任务，推荐待补充知识
```

---

#### 3.2 人工主导场景

**场景1: 质检标准调整**
- **人工职责**: 制定和调整质检标准（如合规性评分规则）
- **AI职责**: 按照新标准执行质检
- **协作方式**: 人工配置 → AI执行 → 人工验证

**示例**:
```
质检专员配置规则:
  - 新增敏感词: "绝对""保证""肯定"
  - 调整响应时间阈值: 优秀<30s, 良好30-60s, 及格60-120s
  ↓
InspectorAgent更新评分逻辑
  ↓
对历史对话重新评分
  ↓
质检专员抽查验证新规则准确性
```

---

**场景2: 争议判定**
- **人工职责**: 处理客服对质检结果的申诉
- **AI职责**: 提供原始依据和参考意见
- **协作方式**: 客服申诉 → AI提供证据 → 人工终审

**示例**:
```
客服张三申诉: "我觉得友好度评分不公平"
  ↓
InspectorAgent提供证据:
  - 原始对话记录
  - 评分依据: "第3、5轮回复未使用礼貌用语"
  - 对比数据: 团队平均友好度8.5，张三7.2
  ↓
质检专员王经理复核
  ↓
判定:
  - 确实存在礼貌用语缺失
  - 但考虑到故障紧急性，酌情加0.5分
  - 最终得分: 7.7
  ↓
InspectorAgent记录: 紧急故障场景下，友好度权重降低
```

---

#### 3.3 协作模式场景

**场景1: 报告生成**
- **AI职责**: 自动聚合数据，生成报告草稿
- **人工职责**: 审核报告，补充分析和建议
- **协作方式**: AI草稿 → 人工编辑 → AI排版

**示例**:
```
InspectorAgent生成月报草稿
  ↓
包含: 数据统计、图表、趋势分析
  ↓
团队经理审核:
  - 补充: 本月团队建设活动对满意度提升的影响
  - 修改: 将"建议增加人手"改为"优化排班"
  - 删除: 删除敏感的个人低分数据
  ↓
InspectorAgent重新排版，生成PDF
```

---

**场景2: 异常检测**
- **AI职责**: 实时监控指标异常
- **人工职责**: 判断异常是否需要干预
- **协作方式**: AI告警 → 人工判断 → AI记录

**示例**:
```
AI检测异常: 客服李四今日满意度突降至3.2 (平均4.5)
  ↓
自动告警推送给团队经理
  ↓
经理查看详情:
  - 原因: 今日接待3个复杂故障，都未能解决
  - 判断: 正常波动，无需干预
  ↓
AI记录: 复杂故障场景下，满意度下降属正常
```

---

### Part 4: 验收标准

#### 4.1 功能验收

**质检功能**:
- [ ] inspectConversation工具响应时间<3秒 (P95)
- [ ] 五维度评分与人工评分差异<1分 (80%样本)
- [ ] 违规检测召回率>95% (无漏报重大违规)
- [ ] 违规检测精确率>85% (误报率<15%)

**批量质检**:
- [ ] batchInspect支持100条对话并发质检
- [ ] 采样策略符合设计 (新员工100%，资深20%)
- [ ] 批量质检结果与单个质检结果一致性>95%

**报告生成**:
- [ ] generateQualityReport生成周报<10秒
- [ ] 报告包含所有必要字段 (summary, scores, issues, recommendations)
- [ ] AI生成的改进建议可用性>80% (人工评估)

**BI看板**:
- [ ] 实时指标刷新延迟<5秒
- [ ] 支持1000+并发用户访问
- [ ] 数据准确性100% (与数据库一致)

**自定义报表**:
- [ ] 支持至少10种维度、20种指标组合
- [ ] Excel导出包含5个Sheet
- [ ] PDF导出样式符合企业规范

---

#### 4.2 性能验收

| 指标 | 目标值 | 测量方法 | 优先级 |
|-----|--------|---------|--------|
| 质检响应时间 | <3秒 (P95) | Prometheus监控 | P0 |
| 批量质检吞吐量 | 100条/次 | 压测 | P0 |
| 报告生成时间 | <10秒 | 前端埋点 | P0 |
| BI看板加载 | <2秒 | 前端埋点 | P1 |
| 导出文件大小 | <10MB | 文件检查 | P1 |
| 并发支持 | 1000用户 | 压测 | P1 |

---

#### 4.3 准确性验收

**质检准确性**:
- [ ] 人工标注100条对话，AI质检准确率≥85%
- [ ] 五维度评分与人工评分相关系数≥0.8
- [ ] 违规检测F1-score≥0.90

**趋势分析准确性**:
- [ ] 趋势预测MAPE (平均绝对百分比误差) <10%
- [ ] 根因分析可解释性>80% (人工评估)

**报告质量**:
- [ ] 报告数据准确性100% (与数据库一致)
- [ ] 改进建议采纳率>60% (管理层反馈)

---

#### 4.4 用户体验验收

**客服端**:
- [ ] 质检结果清晰易懂，无专业术语
- [ ] 改进建议具体可执行
- [ ] 申诉流程便捷，3步内完成

**管理端**:
- [ ] BI看板一屏展示核心指标，无需滚动
- [ ] 自定义报表配置<5分钟完成
- [ ] 报告可直接用于向上级汇报

**用户满意度**:
- [ ] 客服对质检功能满意度≥4.0/5.0
- [ ] 管理员对BI看板满意度≥4.5/5.0

---

#### 4.5 业务验收

**效率提升**:
- [ ] 质检效率提升: 人工10条/小时 → AI 100条/小时 (10倍)
- [ ] 质检覆盖率: 10% → 100% (提升10倍)
- [ ] 报告生成时间: 2小时 → 10秒 (提升720倍)

**质量提升**:
- [ ] 客服质检平均分: 7.5 → 8.3 (提升10.7%)
- [ ] 违规率下降: 8% → 5% (下降37.5%)
- [ ] 客户满意度: 4.2 → 4.5 (提升7.1%)

**成本节省**:
- [ ] 质检人力成本节省: 5人 → 1人 (节省80%)
- [ ] 报告制作成本节省: 2小时/周 → 0 (节省100%)

---

## 附录

### A. 质检评分细则

#### A.1 响应速度评分标准

```python
def score_response_speed(avg_response_time):
    """响应速度评分 (0-10)"""
    if avg_response_time < 30:
        return 10.0
    elif avg_response_time < 60:
        return 9.0
    elif avg_response_time < 120:
        return 8.0
    elif avg_response_time < 180:
        return 7.0
    elif avg_response_time < 300:
        return 6.0
    else:
        return max(5.0 - (avg_response_time - 300) / 60, 0)
```

---

#### A.2 专业性评分标准

```python
def score_professionalism(conversation):
    """专业性评分 (0-10)"""
    score = 10.0

    # 1. 知识引用 (+1分)
    if conversation.knowledgeReferences > 0:
        score += 1.0

    # 2. 术语使用准确性
    technical_terms = extract_technical_terms(conversation)
    accuracy = verify_term_accuracy(technical_terms)
    score += accuracy * 2.0  # 最多+2分

    # 3. 解决方案完整性
    if has_complete_solution(conversation):
        score += 2.0

    # 4. 故障诊断逻辑性
    if is_logical_diagnosis(conversation):
        score += 1.0

    # 扣分项
    if has_vague_answer(conversation):  # 模糊回答
        score -= 2.0

    if has_factual_error(conversation):  # 事实错误
        score -= 3.0

    return max(min(score, 10.0), 0.0)
```

---

#### A.3 友好度评分标准

```python
def score_friendliness(conversation):
    """友好度评分 (0-10)"""
    score = 8.0  # 基准分

    # 加分项
    if has_greeting(conversation):  # 问候语
        score += 0.5

    if uses_customer_name(conversation):  # 称呼客户姓名
        score += 0.5

    if uses_emoji(conversation):  # 使用表情符号
        score += 0.5

    if has_empathy(conversation):  # 表达同理心
        score += 1.0

    if has_closing(conversation):  # 礼貌结束语
        score += 0.5

    # 扣分项
    if uses_rude_language(conversation):  # 不礼貌用语
        score -= 3.0

    if too_cold_tone(conversation):  # 语气冷漠
        score -= 1.0

    if interrupts_customer(conversation):  # 打断客户
        score -= 0.5

    return max(min(score, 10.0), 0.0)
```

---

#### A.4 合规性评分标准

```python
def score_compliance(conversation):
    """合规性评分 (0-10)"""
    score = 10.0  # 默认满分

    # 违规检测
    violations = detect_violations(conversation)

    for violation in violations:
        if violation.type == "sensitive_word":  # 敏感词
            score -= 2.0
        elif violation.type == "overpromise":  # 承诺超限
            score -= 3.0
        elif violation.type == "info_leak":  # 信息泄露
            score -= 5.0
        elif violation.type == "discriminatory":  # 歧视性语言
            score -= 10.0  # 直接0分

    return max(score, 0.0)
```

---

#### A.5 解决率评分标准

```python
def score_resolution_rate(conversation):
    """解决率评分 (0-10)"""
    if conversation.status == "resolved":
        if conversation.resolvedInFirstResponse:
            return 10.0  # 一次解决
        elif conversation.turnCount <= 3:
            return 9.0   # 3轮内解决
        elif conversation.turnCount <= 5:
            return 8.0   # 5轮内解决
        else:
            return 7.0   # 5轮以上解决

    elif conversation.status == "escalated":
        return 6.0  # 升级到人工

    elif conversation.status == "pending":
        return 5.0  # 待处理

    else:  # unresolved
        return 0.0  # 未解决
```

---

### B. Prometheus监控指标

```prometheus
# 质检指标
inspector_inspections_total{status="success|failed"} counter
inspector_inspection_duration_seconds{quantile="0.5|0.95|0.99"} histogram
inspector_average_score{dimension="overall|speed|professionalism|..."} gauge

# 违规指标
inspector_violations_total{type="sensitive_word|overpromise|..."} counter
inspector_violation_rate gauge

# 报告生成指标
inspector_report_generation_duration_seconds histogram
inspector_reports_generated_total counter

# BI看板指标
dashboard_page_load_duration_seconds histogram
dashboard_concurrent_users gauge
dashboard_api_requests_total counter
```

---

### C. 数据库索引优化

```sql
-- Inspection表索引
CREATE INDEX idx_inspection_conversation ON inspections(conversation_id);
CREATE INDEX idx_inspection_date ON inspections(inspected_at);
CREATE INDEX idx_inspection_csr ON inspections(csr_id);
CREATE INDEX idx_inspection_score ON inspections(overall_score);

-- 复合索引（用于排名查询）
CREATE INDEX idx_inspection_csr_date_score ON inspections(csr_id, inspected_at DESC, overall_score DESC);

-- 分区表（按月分区）
CREATE TABLE inspections_202512 PARTITION OF inspections
FOR VALUES FROM ('2025-12-01') TO ('2026-01-01');
```

---

**文档结束**

**相关文档**:
- InspectorAgent PRD: `3-agents/InspectorAgent-PRD.md`
- 产品概述: `1-overview/Product-Overview.md`
- v0.8增量PRD: `../../3-incremental/PRD-v0.8-Incremental.md`
- v1.0增量PRD: `../../3-incremental/PRD-v1.0-Incremental.md`
